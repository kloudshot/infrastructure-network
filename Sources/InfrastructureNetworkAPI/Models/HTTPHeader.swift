public enum HTTPHeader {
    public enum Key {
        public static var contentType = "Content-Type"
        public static var authorization = "Authorization"
    }
    
    public enum Value {
        public static var applicationJSON = "application/json"
    }
}
